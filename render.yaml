services:
  - type: web
    name: conciliacao-clinica
    env: python
    plan: free
    pythonVersion: "3.12.0"
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
